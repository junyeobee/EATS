<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eats.mapper.store.SalesMapper">
	<insert id="insertSalesWithDetails" parameterType="com.eats.store.model.SalesSaveDTO">
		<selectKey keyProperty="sellIdx" resultType="int" order="BEFORE">
		    SELECT SELL_IDX_SEQ.NEXTVAL FROM DUAL
		</selectKey>
        INSERT 
        	ALL
        INTO 
        	SELL (
				SELL_IDX,
				STORE_IDX,
				SELL_DATE,
				SELL_STAT,
				TOTAL_CNT,
				SELL_METHOD
        	) VALUES (
				#{sellIdx},
				#{storeIdx},
				#{sellDate},
				1,
				#{totalCnt},
				#{sellMethod}
			)
		<foreach collection="details" item="detail">
			INTO SELL_DETAIL (
				SELL_D_IDX,
				SELL_IDX,
				MENU_IDX,
				ORDER_NUM
			) VALUES (
				SELL_D_IDX_SEQ.NEXTVAL,
				#{sellIdx},
				#{detail.menuIdx},
				#{detail.orderNum}
			)
		</foreach>
		SELECT * FROM DUAL
    </insert>
</mapper>