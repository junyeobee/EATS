<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.eats.mapper.store.LoginMapper">
  
  
  	<select id="storeLogin" parameterType="String" resultType="com.eats.store.model.EatsStoreDTO">
  		select 
  		store_pwd,store_idx
  		from 
  		store_account
  		where
  		store_id=#{storeId}  		
  	</select>
  	
  	
  	<select id="findId"  parameterType="com.eats.store.model.EatsStoreDTO" resultType="String">
  		select store_id
  		from store_account
  		where
  		store_email=#{store_email} 
  		and
  		store_ceo=#{store_ceo}		
  	</select>
  
  
  	<select id="storeidCheckForFindPwd">
		SELECT
			STORE_EMAIL
		FROM
			store_account
		WHERE
			STORE_ID=#{storeId}
	</select>
	
	
	<update id="storeUpdatePwd" parameterType="map">
	UPDATE
		store_account
	set
		store_pwd=#{newPwd}
	where
		store_id=#{storeId}
	</update>
	
	
	<select id="storeInfo" parameterType="String" resultType="String">
		select 
			store_ceo
		from
			store_acount
		where 
			store_id=#{storeId}
	
	</select>
	
	
  </mapper>