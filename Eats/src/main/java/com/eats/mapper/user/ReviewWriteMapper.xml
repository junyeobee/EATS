<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eats.mapper.user.ReviewWriteMapper">
	<select id="getStoreInfoByReserveIdx">
		SELECT
			S.*, SI.STORE_IMG
		FROM
			STORE S, STORE_IMG SI
		WHERE
			S.STORE_IDX = (
				SELECT
					STORE_IDX
				FROM 
					RESERVATION
				WHERE 
					RESERVE_IDX=#{reserve_idx}
			)
		AND
			S.STORE_IDX = SI.STORE_IDX
		AND
			SI.IMG_ORDER = 1
	</select>
	<select id="getReserveInfo">
		SELECT
			*
		FROM
			RESERVATION
		WHERE
			RESERVE_IDX = #{reserve_idx}
	</select>
	
</mapper>